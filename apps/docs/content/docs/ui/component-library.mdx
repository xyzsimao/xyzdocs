---
title: Component Library
description: The underlying headless component library.
icon: Library
---

## Overview

xyzdocs UI maintains support for both **Base UI** and **Radix UI**, while it uses Radix UI by default.

The configuration may be different if you use xyzdocs CLI for customising components.

### Base UI

You can opt-in to use Base UI by replacing `xyzdoc-ui` with the `@xyzdocs/base-ui` package:

```json title="package.json"
{
  "dependency": {
    "xyzdoc-ui": "npm:@xyzdocs/base-ui@latest"
  }
}
```

For xyzdocs CLI, also configure it in your `cli.json` config:

```json title="cli.json"
{
  "uiLibrary": "base-ui"
}
```
